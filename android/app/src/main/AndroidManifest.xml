<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ADDED THESE TWO LINES FOR PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:label="tourist_safety_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- This is your existing API Key tag -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBVZU3B8ggds2KMSvlYNCIvW87nHoo8KJY" />

        <!-- FIXED: ADDED THIS BLOCK TO FIX THE BLANK MAP -->
        <meta-data
            android:name="com.google.android.gms.maps.renderer.V2.Renderer"
            android:value="latest" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

